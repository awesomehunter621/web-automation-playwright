{
  "name": "web-automation-playwright",
  "version": "1.0.0",
  "private": true,
  "description": "Basic Playwright UI automation tests (JavaScript).",
  "scripts": {
    "test": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.0"
  }
}
